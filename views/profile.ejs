<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/accets/css/dashboard.css" />
    <title>Candidate Profile</title>
  </head>
  <body>
    <div id="left-menu">
      <h1 class="logo-t">Placeholder Logo</h1>
      <ul>
        <li><a href="/profile">Profile</a></li>
        <li><a href="/job-portal">Job Portal</a></li>
        <li><a href="/my-jobs">My Jobs</a></li>
        <li><a href="/auth/logout">Sign Out</a></li>
      </ul>
    </div>
    <div id="content">
      <h1>Edit Profile</h1>

      <img
        id="profile-image"
        src="/uploads/<%= profile.image ? profile.image : 'default-profile-image.jpg' %>"
        alt="Profile Image"
      />

      <form
        action="/profile/update"
        method="post"
        enctype="multipart/form-data"
      >
        <label for="name">Name:</label>
        <input
          placeholder="Enter your name.."
          type="text"
          id="name"
          name="name"
          value="<%= profile.name %>"
          required
        />
        <br />
        <label for="email">Email:</label>
        <input
          type="email"
          id="email"
          name="email"
          value="<%= profile.email %>"
          required
        />
        <br />
        <label for="profileImage">Profile Image:</label>
        <input
          type="file"
          id="profileImage"
          name="profileImage"
          accept="image/*"
        />

        <br />
        <button type="submit">Save Changes</button>
      </form>
    </div>
  </body>
</html>
